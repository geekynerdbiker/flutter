name: artpia
description: Handmade Product Shop

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  intl:
  image: ^2.1.19
  provider: ^4.3.3
  image_picker: ^0.6.7
  firebase_auth: ^0.20.0+1
  firebase_core: ^0.7.0
  path_provider: ^1.6.27
  cupertino_icons: ^1.0.0
  cloud_firestore: ^0.16.0
  firebase_storage: ^7.0.0
  shared_preferences: ^0.5.12
  flutter_staggered_grid_view: ^0.3.4

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true